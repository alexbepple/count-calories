{
  "scripts": {
    "start":
      "cross-env NODE_ENV=development nodemon --ignore storage.json -x 'wt serve --storage-file storage.json' .",
    "start_once": "wt serve .",
    "deploy": "wt create --secrets-file secrets.ini --name 'count-calories' ."
  },
  "private": true,
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "ramda": "^0.25.0",
    "webtask-tools": "^3.2.0"
  },
  "devDependencies": {
    "cross-env": "^5.1.3",
    "nodemon": "^1.17.1",
    "wt-cli": "^8.3.1"
  }
}
