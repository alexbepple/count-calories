{
  apps: [
    {
      name: "webtask",
      interpreter: "wt",
      interpreter_args: "serve --storage-file storage.json",
      script: "src/",
      env: {
        NODE_ENV: "development"
      },
      ignore_watch: ["storage.json"]
    }
  ]
}
